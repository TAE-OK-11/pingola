name: verify-and-publish

concurrency:
  group: pingora-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      publish_images:
        description: Publish the generic and Oracle Zen 1 GHCR images from this manual run
        required: false
        default: false
        type: boolean
  push:
    branches:
      - main
    tags:
      - "v*"
  pull_request:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      checks: write
    steps:
      - uses: actions/checkout@v7
      - name: RustSec dependency audit
        uses: rustsec/audit-check@v2.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - name: Install build and test tools
        run: |
          sudo apt-get update
          sudo apt-get install --yes cmake libcap2-bin ninja-build jq nghttp2-client
          rustup toolchain install 1.97.1 --profile minimal --component clippy,rustfmt
          rustup show active-toolchain
      - name: PGO trainer preflight
        run: |
          bash -n bench/pgo_train.sh
          bash -n bench/pgo_compare_three.sh
          rustc --edition=2021 -D warnings bench/backend.rs -o /tmp/pgo-backend
          rustc --edition=2021 -D warnings bench/pgo_client.rs -o /tmp/pgo-client
          /tmp/pgo-client \
            --port 80 \
            --threads 0 \
            --requests-per-thread 1 \
            --requests-per-connection 500 \
            2>/tmp/pgo-client-validation.log && exit 1 || true
          grep -q 'threads must be 1..=64' /tmp/pgo-client-validation.log
      - name: Format
        run: cargo fmt --check
      - name: AWS-LC-only TLS dependency graph
        run: |
          cargo tree --locked > /tmp/cargo-tree.txt
          grep -q 'aws-lc-rs v' /tmp/cargo-tree.txt
          grep -q 'rustls v' /tmp/cargo-tree.txt
          if grep -Eiq '(^|[[:space:]])(boring|boring-sys|pingora-boringssl) v' /tmp/cargo-tree.txt; then
            echo 'BoringSSL unexpectedly appears in the selected dependency graph' >&2
            exit 1
          fi
      - name: Unit tests
        run: cargo test --all-targets --locked
      - name: Build integration-test binary
        run: |
          cargo build --locked
          # Functional listener tests intentionally exercise production ports
          # 80/443 while the runner itself remains unprivileged.
          sudo setcap cap_net_bind_service=+ep target/debug/pingora
      - name: Lints
        run: cargo clippy --all-targets --locked -- -D warnings
      - name: System allocator baseline still builds
        run: cargo check --all-targets --locked --no-default-features --features system-allocator,tls-aws-lc
      - name: Jemalloc rollback build still compiles
        run: cargo check --all-targets --locked --no-default-features --features jemalloc,tls-aws-lc
      - name: Protocol and security integration checks
        run: |
          # This test intentionally creates a root-owned mode-0600 private key
          # and then checks it as UID/GID 10001. Limit elevation to that test.
          sudo tests/runtime_preflight.sh
          tests/listeners_health.sh
          tests/retries.sh
          tests/limit_isolation.sh
          tests/static_global_limit.sh
          tests/service_matrix.sh
          tests/integration.sh
      - name: HTTP/2 correctness matrix
        run: tests/http2_matrix.sh

  image-test:
    if: github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && inputs.publish_images != true)
    needs: [audit, verify]
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - target_cpu: x86-64-v2
            lto: fat
            pgo_mode: train
            pgo_train_target_cpu: x86-64-v2
            runtime: true
          - target_cpu: znver1
            lto: fat
            pgo_mode: train
            pgo_train_target_cpu: x86-64-v2
            runtime: false
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - name: Build image
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: pingora:ci-${{ matrix.target_cpu }}-${{ matrix.lto }}-${{ matrix.pgo_mode }}
          pull: true
          build-args: |
            ALLOCATOR=tcmalloc
            TLS_PROVIDER=aws-lc
            PGO_MODE=${{ matrix.pgo_mode }}
            PGO_TRAIN_TARGET_CPU=${{ matrix.pgo_train_target_cpu }}
            RUST_TARGET_CPU=${{ matrix.target_cpu }}
            RUST_LTO=${{ matrix.lto }}
            BUILD_VERSION=ci
            BUILD_REVISION=${{ github.sha }}
          cache-from: type=gha,scope=image-test-${{ matrix.target_cpu }}
          cache-to: type=gha,mode=max,scope=image-test-${{ matrix.target_cpu }}
      - name: Non-root and read-only runtime checks
        if: matrix.runtime
        env:
          PINGORA_TEST_IMAGE: pingora:ci-${{ matrix.target_cpu }}-${{ matrix.lto }}-${{ matrix.pgo_mode }}
          PINGORA_EXPECTED_TARGET_CPU: ${{ matrix.target_cpu }}
          PINGORA_EXPECTED_LTO: ${{ matrix.lto }}
          PINGORA_EXPECTED_TLS_PROVIDER: aws-lc
          PINGORA_EXPECTED_PGO: ${{ matrix.pgo_mode }}
        run: tests/docker_runtime.sh
      - name: Specialized image metadata check
        if: ${{ !matrix.runtime }}
        env:
          IMAGE: pingora:ci-${{ matrix.target_cpu }}-${{ matrix.lto }}-${{ matrix.pgo_mode }}
        run: |
          # GitHub-hosted runner CPU models are not stable. Running the full
          # znver1 listener on an incompatible host can correctly SIGILL even
          # though the image was built for a compatible Oracle/AMD Zen host.
          test "$(docker image inspect --format '{{.Config.User}}' "${IMAGE}")" = "10001:10001"
          target_cpu=$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.rust.target-cpu"]')
          allocator=$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.allocator"]')
          test "${target_cpu}" = "znver1"
          test "${allocator}" = "tcmalloc"
          test "$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.rust.lto"]')" = "fat"
          test "$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.rust.pgo"]')" = "${{ matrix.pgo_mode }}"

  image:
    # Both production variants are reproducibly built by GitHub Actions. The
    # generic image targets x86-64-v2; the Oracle image targets AMD Zen 1
    # explicitly rather than inheriting the hosted runner's native CPU.
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.publish_images)
    needs: [audit, verify]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - name: Sign in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          # Publish the deprecated name from the same build for one migration
          # release so both tags resolve to identical manifests.
          images: |
            ghcr.io/tae-ok-11/pingora
            ghcr.io/tae-ok-11/pingola
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
          # OCI source/url must resolve to the repository that produced the
          # image. Change both to /pingora atomically when the GitHub repository
          # itself is renamed; the product and image names are already Pingora.
          labels: |
            org.opencontainers.image.title=Pingora
            org.opencontainers.image.description=High-performance AWS-LC JBS Pingora reverse proxy
            org.opencontainers.image.source=https://github.com/TAE-OK-11/pingola
            org.opencontainers.image.url=https://github.com/TAE-OK-11/pingola
            org.opencontainers.image.allocator=tcmalloc
            org.opencontainers.image.tls.provider=aws-lc
      - name: Build and publish
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: true
          pull: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            ALLOCATOR=tcmalloc
            TLS_PROVIDER=aws-lc
            PGO_MODE=train
            PGO_TRAIN_TARGET_CPU=x86-64-v2
            RUST_TARGET_CPU=x86-64-v2
            RUST_LTO=fat
            BUILD_VERSION=${{ steps.meta.outputs.version }}
            BUILD_REVISION=${{ github.sha }}
          cache-from: type=gha,scope=image-generic-x86-64-v2
          cache-to: type=gha,mode=max,scope=image-generic-x86-64-v2
          provenance: mode=max
          sbom: true
      - name: Verify the exact published generic image
        env:
          PINGORA_TEST_IMAGE: ghcr.io/tae-ok-11/pingora@${{ steps.build.outputs.digest }}
          PINGORA_EXPECTED_TARGET_CPU: x86-64-v2
          PINGORA_EXPECTED_LTO: fat
          PINGORA_EXPECTED_TLS_PROVIDER: aws-lc
          PINGORA_EXPECTED_PGO: train
        run: tests/docker_runtime.sh

  image-oracle-zen1:
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.publish_images)
    needs: [audit, verify]
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - name: Sign in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Oracle Zen 1 image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/tae-ok-11/pingora
            ghcr.io/tae-ok-11/pingola
          tags: |
            type=raw,value=oracle-zen1
            type=raw,value=pgo-znver1
            type=sha,prefix=sha-,suffix=-znver1
            type=semver,pattern={{version}},suffix=-znver1
          labels: |
            org.opencontainers.image.title=Pingora (Oracle Zen 1)
            org.opencontainers.image.description=AMD Zen 1 optimized AWS-LC JBS Pingora reverse proxy
            org.opencontainers.image.source=https://github.com/TAE-OK-11/pingola
            org.opencontainers.image.url=https://github.com/TAE-OK-11/pingola
            org.opencontainers.image.allocator=tcmalloc
            org.opencontainers.image.tls.provider=aws-lc
      - name: Build and publish Oracle Zen 1 image
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          platforms: linux/amd64
          push: true
          pull: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            ALLOCATOR=tcmalloc
            TLS_PROVIDER=aws-lc
            PGO_MODE=train
            PGO_TRAIN_TARGET_CPU=x86-64-v2
            RUST_TARGET_CPU=znver1
            RUST_LTO=fat
            BUILD_VERSION=${{ steps.meta.outputs.version }}-znver1
            BUILD_REVISION=${{ github.sha }}
          cache-from: type=gha,scope=image-oracle-zen1
          cache-to: type=gha,mode=max,scope=image-oracle-zen1
          provenance: mode=max
          sbom: true
      - name: Verify published Oracle image metadata
        env:
          IMAGE: ghcr.io/tae-ok-11/pingora@${{ steps.build.outputs.digest }}
        run: |
          docker pull "${IMAGE}"
          test "$(docker image inspect --format '{{.Config.User}}' "${IMAGE}")" = "10001:10001"
          test "$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.rust.target-cpu"]')" = "znver1"
          test "$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.rust.lto"]')" = "fat"
          test "$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.rust.pgo"]')" = "train"
          test "$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.allocator"]')" = "tcmalloc"
          test "$(docker image inspect "${IMAGE}" | jq -r '.[0].Config.Labels["org.opencontainers.image.tls.provider"]')" = "aws-lc"
